/**
 * Users can register.
 * Users can recieve messages via email.
 * User can view their own profile (image).
 * User can view course list.
 * Timed messages can be sent according to CommsSchedule (single transmission after event).
 * User can view sent messages as HTML.
 * PDF document can be downloaded after an event.
 **/

